package mx.codeInter;
/*


import org.junit.Test;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.test.context.SpringBootTest;
import org.springframework.boot.test.web.client.TestRestTemplate;
import org.springframework.boot.web.server.LocalServerPort;

import static junit.framework.TestCase.assertEquals;

@SpringBootTest(webEnvironment = SpringBootTest.WebEnvironment.RANDOM_PORT)
public class HTTPResponseT {
    @LocalServerPort
    private int port;

    @Autowired
    private TestRestTemplate testrestTemplate ;

    @Test
    public void shouldPassIfStringMatches(){
        //System.out.println(testrestTemplate.getForObject("http://localhost:"+port+"/", String.class));
        assertEquals("MainApi de Spring Boot",
                testrestTemplate.getForObject("http://localhost:"+port+"/", String.class));
    }
}
*/

